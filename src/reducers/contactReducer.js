// reducer for Contact component actions?

const initialState= {};


export default function(state=initialState, action){
  // how reducer interacts with state depends on the type of action passed to it
  switch(action.type) {
    default:
      return state;
  }
}
